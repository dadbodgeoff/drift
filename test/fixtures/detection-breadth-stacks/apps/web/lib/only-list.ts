// D-S2 symptom (b): a file exporting ONLY by bare list. Before W7 it had no key at all, so nothing
// anywhere said the module had not been understood.
const REGION = "eu";

function regionOf() {
  return REGION;
}

export { REGION, regionOf };
