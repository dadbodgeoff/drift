# Detection breadth notes

D-PA1: this is Markdown under a `.ts` extension. Before W7 the scan produced facts from it and
reported no diagnostic at all, so nothing downstream could tell it had not been read.

- one
- two
