{
  "note": "Mutation recipes as DATA, not as lambdas inside a generator. Three separate attempts to patch these as Python source silently produced the wrong recipe list - once through re.sub eating an escape, once through a replacement landing on the wrong closing bracket. A recipe that is quietly wrong produces a mutation suite that quietly tests less than it claims, which is the exact failure mutation testing exists to prevent.",
  "recipes": [
    { "id": "alias-fixpoint-off", "file": "crates/drift-engine/src/rules.rs",
      "find": "for _ in 0..ALIAS_ROUNDS {", "op": "replace",
      "from": "0..ALIAS_ROUNDS", "to": "0..0",
      "rationale": "the alias/barrel fixpoint never runs: every re-exported import goes unresolved",
      "kills_expected": ["pnpm test:engine", "pnpm eval:evasion", "pnpm eval:breadth"] },
    { "id": "alias-one-hop", "file": "crates/drift-engine/src/rules.rs",
      "find": "const ALIAS_ROUNDS: usize = 8;", "op": "set",
      "to": "const ALIAS_ROUNDS: usize = 1;",
      "rationale": "barrel resolution stops after one hop: 2-hop re-exports silently escape",
      "kills_expected": ["pnpm test:engine", "pnpm eval:evasion"] },
    { "id": "symbol-identity-off", "file": "crates/drift-engine/src/security_patterns.rs",
      "find": "fn imported_symbol_matches(facts: &[Fact], local_name: &str, accepted_symbol: &str) -> bool {",
      "op": "append", "to": "\n    if true { return true; }",
      "rationale": "every imported symbol matches the accepted helper: symbol identity disabled",
      "kills_expected": ["pnpm test:engine", "pnpm test:e2e", "pnpm eval:presence"] },
    { "id": "helper-module-any", "file": "crates/drift-engine/src/security_patterns.rs",
      "find": "fn helper_import_matches(fact: &Fact, contract: Option<&AcceptedHelperImport>) -> bool {",
      "op": "append", "to": "\n    if true { return true; }",
      "rationale": "a helper from ANY module satisfies the contract - the defect S4 fixed, re-injected",
      "kills_expected": ["pnpm test:engine", "pnpm test:e2e", "pnpm eval:presence"] },
    { "id": "helper-spelling-any", "file": "crates/drift-engine/src/security_patterns.rs",
      "find": "    if spelling == expected_specifier {", "op": "replace",
      "from": "spelling == expected_specifier", "to": "true",
      "rationale": "any spelling is accepted as the helper's module - S4's module identity disabled",
      "kills_expected": ["pnpm test:engine", "pnpm eval:presence"] },
    { "id": "repo-resolved-any-file", "file": "crates/drift-engine/src/security_patterns.rs",
      "find": "                .any(|file| module_key(file) == candidate)", "op": "replace",
      "from": "module_key(file) == candidate", "to": "true",
      "rationale": "RepoResolved identity accepts any resolved file, not the one the spelling names",
      "kills_expected": ["pnpm test:engine", "pnpm eval:presence"] },
    { "id": "scope-matches-nothing", "file": "crates/drift-engine/src/check_command.rs",
      "find": "fn path_glob_matches(pattern: &str, file_path: &str) -> bool {",
      "op": "append", "to": "\n    if true { return false; }",
      "rationale": "no file ever matches a convention scope: every check runs on zero inputs and passes",
      "kills_expected": ["pnpm test:e2e", "pnpm check:cell-ledger", "pnpm eval:presence"] },
    { "id": "phase5-scope-everything", "file": "crates/drift-engine/src/check_command.rs",
      "find": "fn phase5_file_scope_matches(file_path: &str, path_globs: &[String]) -> bool {",
      "op": "append", "to": "\n    if true { return true; }",
      "rationale": "every file is in scope for every phase5 convention: precision should collapse",
      "kills_expected": ["pnpm test:engine", "pnpm eval:presence"] }
  ]
}
